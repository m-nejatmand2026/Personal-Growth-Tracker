# Shared domain directory

This directory intentionally contains no JavaScript business logic.

Growth Compass — Version 1 Beta domain behavior belongs under `worker/modules/<module>/` so each capability owns its own implementation.

This directory remains only as an architecture-gate scan root.
