import{activitiesCapability}from'../capabilities/activities.js';
function escapeHtml(value=''){return String(value).replace(/[&<>"']/g,character=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[character]))}
function goalName(model,goalId){return model.goals.find(goal=>Number(goal.id)===Number(goalId))?.name||'Goal unavailable'}
export async function loadActivities(){const[items,context]=await Promise.all([activitiesCapability.list(),activitiesCapability.creationContext()]);return Object.freeze({items,goals:context.goals||[]})}
function rows(model){if(!model.items.length)return'<section class="static-surface activities-empty"><p class="eyebrow">Activity library</p><h3>No Activities yet</h3><p>Create reusable actions under a Goal. Planning an Activity does not count as completed Progress.</p></section>';return model.items.map(item=>`<article class="static-surface activity-row" data-activity-id="${Number(item.id)}"><div class="activity-row-copy"><p class="eyebrow">${escapeHtml(goalName(model,item.goal_id))}</p><h3>${escapeHtml(item.name)}</h3>${item.description?`<p>${escapeHtml(item.description)}</p>`:'<p class="activity-muted">No description</p>'}</div><div class="activity-row-actions"><button type="button" class="secondary-button" data-activity-edit="${Number(item.id)}">Edit</button><button type="button" class="quiet-button" data-activity-archive="${Number(item.id)}">Archive</button></div></article>`).join('')}
export function renderActivities(model){return`<div class="activities-view"><section class="living-surface activities-hero"><div><p class="eyebrow">Plan · Activities</p><h2>Reusable actions</h2><p>Activities describe what you can do toward a Goal. They are planning structure, not evidence of completion.</p></div><button type="button" class="primary-button" id="newActivity">New activity</button></section><section class="activities-summary" aria-label="Activity summary"><div><span>Active Activities</span><strong>${model.items.length}</strong></div><div><span>Available Goals</span><strong>${model.goals.length}</strong></div></section><div class="activities-list">${rows(model)}</div><section class="static-surface activities-rule"><p class="eyebrow">Contract</p><strong>Activity identity is stable; Progress stays factual.</strong><p>Editing or archiving an Activity changes the reusable planning definition only. Existing factual Progress records are not rewritten.</p></section></div>`}
function editorMarkup(model,item=null){const editing=Boolean(item);return`<div class="activity-modal-backdrop" id="activityModalBackdrop"><section class="activity-editor" role="dialog" aria-modal="true" aria-labelledby="activityEditorTitle"><header><div><p class="eyebrow">${editing?'Edit':'New'} Activity</p><h2 id="activityEditorTitle">${editing?'Edit reusable action':'Create reusable action'}</h2></div><button type="button" class="activity-editor-close" id="activityEditorClose" aria-label="Close Activity editor">×</button></header><form id="activityEditorForm"><label>Activity name<input id="activityName" name="name" maxlength="120" required value="${escapeHtml(item?.name||'')}"></label><label>Goal<select id="activityGoal" name="goal_id" required><option value="">Choose a Goal</option>${model.goals.map(goal=>`<option value="${Number(goal.id)}"${Number(item?.goal_id)===Number(goal.id)?' selected':''}>${escapeHtml(goal.name)}</option>`).join('')}</select></label><label>Description <span>optional</span><textarea id="activityDescription" name="description" maxlength="1000" rows="4">${escapeHtml(item?.description||'')}</textarea></label><p class="activity-form-error" id="activityFormError" role="alert"></p><footer><button type="button" class="secondary-button" id="activityEditorCancel">Cancel</button><button type="submit" class="primary-button">${editing?'Save changes':'Create activity'}</button></footer></form></section></div>`}
function archiveMarkup(item){return`<div class="activity-modal-backdrop" id="activityModalBackdrop"><section class="activity-editor activity-archive-dialog" role="dialog" aria-modal="true" aria-labelledby="activityArchiveTitle"><header><div><p class="eyebrow">Archive Activity</p><h2 id="activityArchiveTitle">Archive ${escapeHtml(item.name)}?</h2></div><button type="button" class="activity-editor-close" id="activityEditorClose" aria-label="Close archive dialog">×</button></header><p>This removes the Activity from future planning choices. Existing factual Progress remains unchanged.</p><footer><button type="button" class="secondary-button" id="activityEditorCancel">Keep activity</button><button type="button" class="danger-button" id="activityArchiveConfirm">Archive</button></footer></section></div>`}
export function bindActivities(model,{reload}={}){const overlay=document.querySelector('#overlayHost');let previousFocus=null;let keyHandler=null;const close=()=>{if(!overlay)return;overlay.innerHTML='';document.removeEventListener('keydown',keyHandler);keyHandler=null;previousFocus?.focus?.();};const trap=event=>{if(event.key==='Escape'){event.preventDefault();close();return;}if(event.key!=='Tab')return;const nodes=[...overlay.querySelectorAll('button,input,select,textarea')].filter(node=>!node.disabled);if(!nodes.length)return;const first=nodes[0],last=nodes.at(-1);if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus();}};const mount=html=>{previousFocus=document.activeElement;overlay.innerHTML=html;keyHandler=trap;document.addEventListener('keydown',keyHandler);overlay.querySelector('#activityEditorClose')?.addEventListener('click',close);overlay.querySelector('#activityEditorCancel')?.addEventListener('click',close);overlay.querySelector('#activityModalBackdrop')?.addEventListener('click',event=>{if(event.target.id==='activityModalBackdrop')close()});overlay.querySelector('input,button')?.focus();};const openEditor=item=>{if(!model.goals.length)return;mount(editorMarkup(model,item));const form=overlay.querySelector('#activityEditorForm');form?.addEventListener('submit',async event=>{event.preventDefault();const error=overlay.querySelector('#activityFormError');const payload={name:form.elements.name.value.trim(),goal_id:Number(form.elements.goal_id.value),description:form.elements.description.value.trim()||null};if(!payload.name||!Number.isInteger(payload.goal_id)||payload.goal_id<=0){error.textContent='Choose a Goal and enter an Activity name.';return;}try{if(item)await activitiesCapability.update(item.id,payload);else await activitiesCapability.create(payload);close();await reload?.();}catch(reason){error.textContent=reason?.message||'Could not save Activity.';}})};document.querySelector('#newActivity')?.addEventListener('click',()=>openEditor(null));document.querySelectorAll('[data-activity-edit]').forEach(button=>button.addEventListener('click',()=>{const item=model.items.find(candidate=>Number(candidate.id)===Number(button.dataset.activityEdit));if(item)openEditor(item)}));document.querySelectorAll('[data-activity-archive]').forEach(button=>button.addEventListener('click',()=>{const item=model.items.find(candidate=>Number(candidate.id)===Number(button.dataset.activityArchive));if(!item)return;mount(archiveMarkup(item));overlay.querySelector('#activityArchiveConfirm')?.addEventListener('click',async()=>{try{await activitiesCapability.archive(item.id);close();await reload?.();}catch(reason){const dialog=overlay.querySelector('.activity-archive-dialog');dialog?.insertAdjacentHTML('beforeend',`<p class="activity-form-error" role="alert">${escapeHtml(reason?.message||'Could not archive Activity.')}</p>`);}})}));}
