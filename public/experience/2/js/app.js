import{readPreference,writePreference}from'./core/preferences.js';import{renderFoundation}from'./views/foundation.js';import{loadToday,renderToday,bindToday}from'./views/today.js';import{loadPlan,renderPlan,bindPlan}from'./views/plan.js';import{loadGoals,renderGoals,bindGoals}from'./views/goals.js';import{loadActivities,renderActivities,bindActivities}from'./views/activities.js';import{loadSchedule,renderSchedule,bindSchedule}from'./views/schedule.js';import{loadProgress,renderProgress,bindProgress}from'./views/progress.js';import{loadInsights,renderInsights}from'./views/insights.js';import{loadJournal,renderJournal,bindJournal}from'./views/journal.js';import{renderWellness,bindWellness,deactivateWellness}from'./views/wellness.js';import{renderSettings,bindSettings,applyPresentationPreferences}from'./views/settings.js';import{createLogger}from'./views/logger.js';
applyPresentationPreferences();if(!document.querySelector('link[href="/experience/2/css/time-planning.css"]')){const link=document.createElement('link');link.rel='stylesheet';link.href='/experience/2/css/time-planning.css';document.head.append(link)}
const views=new Set(['today','plan','goals','activities','schedule','progress','insights','wellness','journal','settings']);const titles={today:'Today',plan:'Plan',goals:'Direction',activities:'Activities',schedule:'Schedule',progress:'Progress',insights:'Insights',wellness:'Wellness',journal:'Journal',settings:'Settings'};let current=readPreference('last-view','today');if(!views.has(current))current='today';let renderVersion=0;
const host=document.querySelector('#viewHost'),title=document.querySelector('#viewTitle'),mobileExploreToggle=document.querySelector('#mobileExploreToggle'),mobileSecondary=document.querySelector('#mobileSecondary');function setMobileExplore(open){if(!mobileExploreToggle||!mobileSecondary)return;const expanded=Boolean(open);mobileExploreToggle.setAttribute('aria-expanded',String(expanded));mobileSecondary.classList.toggle('open',expanded);mobileSecondary.setAttribute('aria-hidden',String(!expanded));mobileSecondary.inert=!expanded}function syncNav(){document.querySelectorAll('[data-view]').forEach(button=>{const active=button.dataset.view===current;button.classList.toggle('active',active);active?button.setAttribute('aria-current','page'):button.removeAttribute('aria-current')})}
function safeMessage(error){return String(error?.message||'Unknown error').replace(/[&<>"']/g,character=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[character]))}
const navigationTransitions=new Set();let loadingTimer=0;function beginViewLoad(version){clearTimeout(loadingTimer);host.setAttribute('aria-busy','true');host.classList.toggle('is-navigation-pending',navigationTransitions.has(version)&&host.childElementCount>0);host.classList.remove('is-loading-delayed','is-navigation-entering');loadingTimer=window.setTimeout(()=>{if(version===renderVersion)host.classList.add('is-loading-delayed')},280)}function finishViewLoad(version,{animate=true}={}){if(version!==renderVersion)return;const wasNavigation=navigationTransitions.delete(version);clearTimeout(loadingTimer);host.removeAttribute('aria-busy');host.classList.remove('is-navigation-pending','is-loading-delayed','is-navigation-entering');if(animate&&wasNavigation){void host.offsetWidth;host.classList.add('is-navigation-entering');host.addEventListener('animationend',()=>host.classList.remove('is-navigation-entering'),{once:true})}if(wasNavigation){document.querySelector('#experience2Main')?.focus({preventScroll:true});window.scrollTo({top:0,behavior:'auto'})}}
let logger=null;async function renderPlanView(period='week',version=renderVersion){beginViewLoad(version);try{const model=await loadPlan({period});if(version!==renderVersion||current!=='plan')return;host.innerHTML=renderPlan(model);bindPlan(model,{reload:nextPeriod=>renderPlanView(nextPeriod,version),navigate:show,openPlanner:prefill=>logger?.open(prefill)});finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='plan')return;host.innerHTML=`<section class="static-surface plan-error"><p class="eyebrow">Plan unavailable</p><h2>Could not load Capacity</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="planRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#planRetry')?.addEventListener('click',()=>void renderPlanView(period,version))}}
async function renderGoalsView(version=renderVersion){beginViewLoad(version);try{const model=await loadGoals();if(version!==renderVersion||current!=='goals')return;host.innerHTML=renderGoals(model);bindGoals(model,{reload:render,navigate:show});finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='goals')return;host.innerHTML=`<section class="static-surface goals-error"><p class="eyebrow">Direction unavailable</p><h2>Could not load Direction</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="goalsRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#goalsRetry')?.addEventListener('click',()=>void renderGoalsView(version))}}
async function renderActivitiesView(version=renderVersion){beginViewLoad(version);try{const model=await loadActivities();if(version!==renderVersion||current!=='activities')return;host.innerHTML=renderActivities(model);bindActivities(model,{reload:()=>renderActivitiesView(version)});finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='activities')return;host.innerHTML=`<section class="static-surface activities-error"><p class="eyebrow">Activities unavailable</p><h2>Could not load reusable actions</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="activitiesRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#activitiesRetry')?.addEventListener('click',()=>void renderActivitiesView(version))}}
async function renderScheduleView(version=renderVersion){beginViewLoad(version);try{const model=await loadSchedule();if(version!==renderVersion||current!=='schedule')return;host.innerHTML=renderSchedule(model);bindSchedule(model,{reload:()=>renderScheduleView(version),navigate:show});finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='schedule')return;host.innerHTML=`<section class="static-surface schedule-error"><p class="eyebrow">Schedule unavailable</p><h2>Could not load recurring commitments</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="scheduleRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#scheduleRetry')?.addEventListener('click',()=>void renderScheduleView(version))}}
async function renderProgressView(version=renderVersion){beginViewLoad(version);try{const model=await loadProgress();if(version!==renderVersion||current!=='progress')return;host.innerHTML=renderProgress(model);bindProgress(model,{reload:render});finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='progress')return;host.innerHTML=`<section class="static-surface progress-error"><p class="eyebrow">Progress unavailable</p><h2>Could not load factual evidence</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="progressRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#progressRetry')?.addEventListener('click',()=>void renderProgressView(version))}}
async function renderInsightsView(version=renderVersion){beginViewLoad(version);try{const model=await loadInsights();if(version!==renderVersion||current!=='insights')return;host.innerHTML=renderInsights(model);finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='insights')return;host.innerHTML=`<section class="static-surface insights-error"><p class="eyebrow">Insights unavailable</p><h2>Could not load evidence</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="insightsRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#insightsRetry')?.addEventListener('click',()=>void renderInsightsView(version))}}
async function renderJournalView(query='',version=renderVersion){beginViewLoad(version);try{const model=await loadJournal(query);if(version!==renderVersion||current!=='journal')return;host.innerHTML=renderJournal(model);bindJournal(model,{reload:nextQuery=>renderJournalView(nextQuery,version)});finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='journal')return;host.innerHTML=`<section class="static-surface journal-error"><p class="eyebrow">Journal unavailable</p><h2>Could not load private reflections</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="journalRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#journalRetry')?.addEventListener('click',()=>void renderJournalView(query,version))}}
function renderWellnessView(version=renderVersion){host.innerHTML=renderWellness();bindWellness({root:host,onRerender:()=>renderWellnessView(version)});finishViewLoad(version)}function renderSettingsView(version=renderVersion){host.innerHTML=renderSettings();bindSettings({rerender:()=>renderSettingsView(version)});finishViewLoad(version)}
async function render(){const version=++renderVersion;beginViewLoad(version);title.textContent=titles[current];document.title=`${titles[current]} — Growth Compass`;syncNav();if(current==='plan'){await renderPlanView('week',version);return}if(current==='goals'){await renderGoalsView(version);return}if(current==='activities'){await renderActivitiesView(version);return}if(current==='schedule'){await renderScheduleView(version);return}if(current==='progress'){await renderProgressView(version);return}if(current==='insights'){await renderInsightsView(version);return}if(current==='journal'){await renderJournalView('',version);return}if(current==='wellness'){renderWellnessView();return}if(current==='settings'){renderSettingsView();return}if(current!=='today'){host.innerHTML=renderFoundation(current);finishViewLoad(version);return}try{const model=await loadToday();if(version!==renderVersion||current!=='today')return;host.innerHTML=renderToday(model);bindToday(model,{reload:render});finishViewLoad(version)}catch(error){if(version!==renderVersion||current!=='today')return;host.innerHTML=`<section class="static-surface today-error"><p class="eyebrow">Today unavailable</p><h2>Could not load your real data</h2><p>${safeMessage(error)}</p><button type="button" class="secondary-button" id="todayRetry">Retry</button></section>`;finishViewLoad(version,{animate:false});document.querySelector('#todayRetry')?.addEventListener('click',()=>void render())}}
logger=createLogger({onSaved:async()=>{if(current==='today'||current==='activities'||current==='progress'||current==='insights'){await render();return}if(current==='plan'){const version=++renderVersion;await renderPlanView('week',version)}}});function show(view){if(!views.has(view))return;setMobileExplore(false);if(view===current)return;if(current==='wellness'&&view!=='wellness')deactivateWellness();current=view;writePreference('last-view',view);navigationTransitions.add(renderVersion+1);void render()}document.querySelectorAll('[data-view]').forEach(button=>button.addEventListener('click',()=>show(button.dataset.view)));document.querySelectorAll('[data-open-add]').forEach(button=>button.addEventListener('click',()=>void logger.open({entryMode:'done'})));mobileExploreToggle?.addEventListener('click',event=>{event.stopPropagation();setMobileExplore(mobileExploreToggle.getAttribute('aria-expanded')!=='true')});mobileSecondary?.addEventListener('click',event=>event.stopPropagation());document.addEventListener('click',()=>setMobileExplore(false));document.addEventListener('keydown',event=>{if(event.key==='Escape'&&mobileExploreToggle?.getAttribute('aria-expanded')==='true'){event.preventDefault();setMobileExplore(false);mobileExploreToggle.focus({preventScroll:true})}});setMobileExplore(false);if('serviceWorker'in navigator){window.addEventListener('load',()=>navigator.serviceWorker.register('/experience/2/sw.js',{scope:'/experience/2/'}).catch(error=>console.warn('Experience 2 service worker registration failed',error)))}void render();
