name: Quality

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact tested commit
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: '24'
          package-manager-cache: false

      - name: Run unit, contract and modularity boundary tests
        run: npm test

      - name: Install pinned Worker integration-test runtime
        run: npm install --no-save --no-package-lock --no-audit --no-fund wrangler@4.123.0

      - name: Run real Worker and isolated D1 integration tests
        run: npm run test:integration
