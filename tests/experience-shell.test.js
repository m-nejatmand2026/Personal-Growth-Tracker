import test from 'node:test';
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const indexHtml=await readFile(new URL('../public/index.html',import.meta.url),'utf8');
const appJs=await readFile(new URL('../public/js/app.js',import.meta.url),'utf8');
const loggerJs=await readFile(new URL('../public/js/modules/logger/ui.js',import.meta.url),'utf8');
const activitiesJs=await readFile(new URL('../public/js/modules/activities/module.js',import.meta.url),'utf8');
const planJs=await readFile(new URL('../public/js/features/plan.js',import.meta.url),'utf8');
const progressJs=await readFile(new URL('../public/js/modules/progress/ui.js',import.meta.url),'utf8');
const progressManifest=await readFile(new URL('../public/js/modules/progress/manifest.js',import.meta.url),'utf8');
const insightsJs=await readFile(new URL('../public/js/modules/insights/ui.js',import.meta.url),'utf8');
const todayJs=await readFile(new URL('../public/js/features/today.js',import.meta.url),'utf8');
const capacityModule=await readFile(new URL('../public/js/modules/capacity/module.js',import.meta.url),'utf8');
const wellbeingModule=await readFile(new URL('../public/js/modules/wellbeing/module.js',import.meta.url),'utf8');
const dailyPlanJs=await readFile(new URL('../public/js/modules/daily-plan/module.js',import.meta.url),'utf8');
const journalJs=await readFile(new URL('../public/js/modules/journal/module.js',import.meta.url),'utf8');
const experienceDoc=await readFile(new URL('../docs/EXPERIENCE_ARCHITECTURE.md',import.meta.url),'utf8');
const planExperience=`${planJs}\n${capacityModule}`;

test('primary navigation matches Version 1 on mobile and desktop',()=>{for(const label of ['Today','Plan','Progress','Insights'])assert.match(indexHtml,new RegExp(`>${label}<|<b>${label}</b>`));assert.match(indexHtml,/id="quickAddBtn"/);assert.match(indexHtml,/class="app-rail"/);assert.match(indexHtml,/data-open-logger/);assert.doesNotMatch(indexHtml,/data-view="week"|data-view="history"/)});
test('Logger supports Plan Start now Done without confusing plans with progress facts',()=>{assert.match(loggerJs,/id="loggerDuration"[^>]*min="1"[^>]*max="1440"/);assert.match(loggerJs,/>Plan</);assert.match(loggerJs,/>Start now</);assert.match(loggerJs,/>Done</);assert.match(loggerJs,/Save progress/);assert.match(loggerJs,/does not count as completed Progress/);assert.match(loggerJs,/Quick repeat/);assert.match(loggerJs,/\/api\/v1\/progress/);assert.doesNotMatch(loggerJs,/\/api\/session/)});
test('Logger Activity and focus UX stays generic behind the Activities public capability',()=>{assert.match(appJs,/moduleRegistry\.get\('activities'\)/);assert.match(appJs,/create\(\{ onSaved: load, activities \}\)/);assert.match(loggerJs,/activityCapability\?\.list/);assert.match(loggerJs,/activityCapability\.create/);assert.doesNotMatch(loggerJs,/\/api\/v1\/activities|\/api\/v1\/goals/);assert.match(activitiesJs,/\/api\/v1\/activities/);assert.match(activitiesJs,/creationContext/);assert.match(loggerJs,/Focus \/ variation/i);assert.doesNotMatch(loggerJs,/Back, Abs, Push-ups|Speaking, Grammar, Vocabulary|Chords, Technique, Song practice/)});
test('Today is a composition surface and business widgets stay module-owned',()=>{assert.match(todayJs,/frontendModules/);assert.match(todayJs,/createFrontendModuleRegistry/);assert.match(todayJs,/dailyPlanPanel/);assert.match(todayJs,/journalPreview/);assert.doesNotMatch(todayJs,/\/api\/v1\/capacity|\/api\/energy|config\/energy|\bENERGY\b/);assert.match(capacityModule,/today-capacity/);assert.match(capacityModule,/\/api\/v1\/capacity\?date=/);assert.match(progressManifest,/todayDirection/);assert.match(progressManifest,/todayRecent/);assert.match(progressManifest,/title: 'Progress direction'/);assert.match(progressManifest,/hasGuidance/);assert.match(progressManifest,/label: 'Actual'/);assert.match(progressManifest,/label: 'Minimum'/);assert.match(progressManifest,/label: 'Target'/);assert.match(progressManifest,/No target set for this period/);assert.match(progressManifest,/\['day', 'week', 'month', 'year'\]/);assert.match(progressManifest,/Activity feed/);assert.match(wellbeingModule,/today-state/);assert.match(wellbeingModule,/\/api\/v1\/wellbeing\/energy/);assert.match(dailyPlanJs,/Today & tomorrow/);assert.match(journalJs,/journal-preview/)});
test('Progress foregrounds factual progress with optional guidance and no catch-up debt',()=>{assert.match(progressJs,/<h2>This week<\/h2>/);assert.match(progressJs,/<h2>Recent<\/h2>/);assert.match(progressJs,/progress-detail-disclosure/);for(const label of ['Actual','Good-enough minimum','Target'])assert.match(progressJs,new RegExp(`>${label}<`));assert.match(progressJs,/Targets are guidance, not debt/);assert.match(progressJs,/no catch-up needed/);assert.match(progressJs,/guidance, not debt/);assert.match(progressJs,/data-delete-progress/);assert.match(progressJs,/Earlier Beta history/);assert.match(progressJs,/Read-only history from the earlier Beta version/);assert.doesNotMatch(progressJs,/data-delete-session/)});
test('Insights keeps evidence thresholds and refuses invented associations',()=>{for(const threshold of ['0–6 days','7–20 days','21–41 days','42+ days'])assert.match(insightsJs,new RegExp(threshold.replace('+','\\+')));assert.match(insightsJs,/Not enough matching wellbeing data yet/);assert.match(insightsJs,/associated with/);assert.match(insightsJs,/does not prove cause/);assert.match(insightsJs,/\/api\/v1\/progress/);assert.match(insightsJs,/\/api\/v1\/wellbeing\/energy/);assert.doesNotMatch(insightsJs,/\/api\/history/);assert.doesNotMatch(insightsJs,/journal/i);assert.doesNotMatch(insightsJs,/causes higher|causes lower|because of sleep/i)});
test('normal Plan UI is human-facing and starts with concrete time reality',()=>{assert.match(planJs,/Plan at a glance/);assert.match(planExperience,/Available this week/);assert.match(planExperience,/Planned this week/);assert.match(planExperience,/Still flexible/);assert.match(planJs,/module\.planSummary/);assert.doesNotMatch(planJs,/Spacious|Balanced|Very full|How full\?|Current plan/i);assert.doesNotMatch(planJs,/independently registered module|explicit contract|failure boundary/i)});
test('experience documentation records Daily Plan Journal and recursive modularity',()=>{assert.match(experienceDoc,/Daily Plan/);assert.match(experienceDoc,/Journal/);assert.match(experienceDoc,/Universal Logger/);assert.match(experienceDoc,/car-parts rule/);assert.match(experienceDoc,/Growth Compass — Version 1 Beta/)});