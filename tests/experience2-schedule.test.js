import test from 'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
const app=await readFile(new URL('../public/experience/2/js/app.js',import.meta.url),'utf8');const view=await readFile(new URL('../public/experience/2/js/views/schedule.js',import.meta.url),'utf8');const lifecycle=await readFile(new URL('../public/experience/2/js/routine-lifecycle.js',import.meta.url),'utf8');const capability=await readFile(new URL('../public/experience/2/js/capabilities/capacity.js',import.meta.url),'utf8');const moduleSource=await readFile(new URL('../worker/modules/capacity/module.js',import.meta.url),'utf8');const plan=await readFile(new URL('../public/experience/2/js/views/plan.js',import.meta.url),'utf8');const css=await readFile(new URL('../public/experience/2/css/schedule.css',import.meta.url),'utf8');const lifecycleCss=await readFile(new URL('../public/experience/2/css/lifecycle.css',import.meta.url),'utf8');const bootstrap=await readFile(new URL('../public/experience/2/js/theme-bootstrap.js',import.meta.url),'utf8');const sw=await readFile(new URL('../public/experience/2/sw.js',import.meta.url),'utf8');
test('Experience 2 Schedule is a real Capacity-owned management destination',()=>{assert.match(app,/loadSchedule\s*,\s*renderSchedule\s*,\s*bindSchedule/);assert.match(app,/current==='schedule'/);assert.match(plan,/data-plan-open="schedule"/);assert.match(view,/capacityCapability\.commitments/);assert.doesNotMatch(view,/\bapi\.|\/v1\/capacity/)});
test('Schedule writes only recurring Capacity commitments',()=>{assert.match(view,/capacityCapability\.create\(payload\)/);assert.match(view,/capacityCapability\.update\(item\.id,payload\)/);assert.match(capability,/\/v1\/capacity\/commitments/);assert.doesNotMatch(view,/progressCapability|\/v1\/progress|dailyPlan/i)});
test('Routine lifecycle is modular and distinguishes reversible archive from permanent removal',()=>{assert.match(bootstrap,/routine-lifecycle/);assert.match(lifecycle,/capacityCapability\.archive\(item\.id\)/);assert.match(lifecycle,/capacityCapability\.restore\(button\.dataset\.routineRestore\)/);assert.match(lifecycle,/capacityCapability\.remove\(item\.id\)/);assert.match(lifecycle,/Archive routine/);assert.match(lifecycle,/Remove permanently/);assert.match(lifecycle,/Factual Progress is not deleted/);assert.match(capability,/archive:id=>api\.delete\(path\(id\)\)/);assert.match(capability,/restore:id=>api\.post\(`\$\{path\(id\)\}\/restore`,\{\}\)/);assert.match(capability,/remove:id=>api\.delete\(`\$\{path\(id\)\}\/permanent`\)/);assert.match(moduleSource,/capacity\\\/commitments\\\/\\d\+\\\/restore/);assert.match(moduleSource,/capacity\\\/commitments\\\/\\d\+\\\/permanent/);assert.doesNotMatch(lifecycle,/progressCapability|\/v1\/progress/);assert.match(lifecycleCss,/routine-lifecycle-actions/)});
test('Routine lifecycle dialog owns its backdrop and keyboard focus boundary',()=>{assert.match(lifecycle,/data-routine-backdrop><section class="activity-editor activity-archive-dialog"[^>]+data-routine-dialog/);assert.match(lifecycle,/if\(event\.target===event\.currentTarget\)close\(\)/);assert.match(lifecycle,/event\.key==='Tab'/);assert.match(lifecycle,/event\.shiftKey&&document\.activeElement===first/);assert.match(lifecycle,/document\.removeEventListener\('keydown',key\)/);assert.match(lifecycle,/opener\?\.focus\?\.\(\{preventScroll:true\}\)/);assert.match(lifecycle,/button\.disabled=true/)});
test('Schedule supports easy weekdays and per-day customization',()=>{assert.match(view,/Easy — one duration for selected days/);assert.match(view,/Customize by day/);assert.match(view,/daily_minutes:daily/);assert.match(view,/weekday_mask:mask/);assert.match(view,/Choose at least one day/)});
test('existing Schedule edits preserve earlier Capacity history by default',()=>{assert.match(view,/version_from:date/);assert.match(view,/Save new version/);assert.match(view,/preserving earlier Capacity history/);assert.doesNotMatch(view,/delete|rewrite earlier/i)});
test('Schedule stays physical-time-only and accessible on phone',()=>{assert.match(view,/Capacity arithmetic only; it never creates Progress/);assert.match(view,/role="dialog" aria-modal="true"/);assert.match(view,/e\.key==='Escape'/);assert.match(view,/e\.key==='Tab'/);assert.match(view,/document\.body\.classList\.add\('schedule-modal-open'\)/);assert.match(view,/document\.body\.classList\.remove\('schedule-modal-open'\)/);assert.match(view,/previous\?\.focus\?\.\(\{preventScroll:true\}\)/);assert.match(lifecycle,/role="dialog" aria-modal="true" aria-labelledby="routineLifecycleTitle"/);assert.match(lifecycle,/event\.key==='Escape'/);assert.match(lifecycle,/event\.key==='Tab'/);assert.match(css,/\.schedule-modal-open\{overflow:hidden\}/);assert.match(css,/@media\(max-width:760px\)/);assert.match(css,/env\(safe-area-inset-bottom\)/)});
test('Plan now consumes Capacity through the Experience 2 capability',()=>{assert.match(plan,/capacityCapability\.summary/);assert.doesNotMatch(plan,/from'\.\.\/core\/api\.js'/);assert.match(capability,/summary:/)});
test('Schedule dependencies are precached in refreshed isolated PWA cache',()=>{assert.match(sw,/growth-compass-preview2-e2-v\d+/);for(const asset of['/experience/2/css/schedule.css','/experience/2/css/lifecycle.css','/experience/2/js/views/schedule.js','/experience/2/js/routine-lifecycle.js','/experience/2/js/capabilities/capacity.js'])assert.ok(sw.includes(`'${asset}'`),asset);assert.doesNotMatch(sw,/experience\/1|preview1/)});