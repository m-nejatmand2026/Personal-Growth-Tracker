import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';
const root=process.cwd();const read=p=>fs.readFileSync(path.join(root,p),'utf8');
function gitBlobSha(file){const data=fs.readFileSync(path.join(root,file));return crypto.createHash('sha1').update(Buffer.from(`blob ${data.length}\0`)).update(data).digest('hex')}
test('Experience 1 frozen recovered frontend blobs remain unchanged',()=>{const expected={'public/experience/1/index.html':'6769ddad7631970a5670f55e5262cb7f0d839946','public/styles.css':'39c3f743bb2362f6bee8b9f39d3658f55a39638c','public/icon.svg':'a7237c74710736bd9934f659b3e07724de832576'};for(const [file,sha]of Object.entries(expected))assert.equal(gitBlobSha(file),sha,`${file} changed from frozen recovered baseline`);assert.match(read('docs/PREVIEW2_MASTER_HANDOFF.md'),/3cf2a65b5918f73e4f182abfdce8589c98d85277/)});
test('Experience frontends do not import sibling frontend resources',()=>{for(const exp of ['1','2']){const dir=path.join(root,'public','experience',exp);const sibling=exp==='1'?'2':'1';for(const file of walk(dir)){if(!/\.(?:html|css|js|mjs)$/.test(file))continue;const source=fs.readFileSync(file,'utf8');const patterns=[new RegExp(`(?:from\\s*|import\\s*\\(|src=|href=|@import\\s*)[\\\"']?[^\\n]*?/experience/${sibling}/`,'i')];for(const pattern of patterns)assert.equal(pattern.test(source),false,`${path.relative(root,file)} imports Experience ${sibling}`)}}});
test('PWA scopes, cache namespaces and preferences are isolated',()=>{const m1=JSON.parse(read('public/experience/1/manifest.webmanifest'));const m2=JSON.parse(read('public/experience/2/manifest.webmanifest'));assert.equal(m1.scope,'/experience/1/');assert.equal(m2.scope,'/experience/2/');assert.notEqual(m1.scope,m2.scope);const sw1=read('public/experience/1/sw.js'),sw2=read('public/experience/2/sw.js');assert.match(sw1,/growth-compass-preview2-e1-/);assert.match(sw2,/growth-compass-preview2-e2-/);assert.doesNotMatch(sw1,/growth-compass-preview2-e2-/);assert.doesNotMatch(sw2,/growth-compass-preview2-e1-/);assert.match(read('public/experience/1/bootstrap.js'),/growth-compass:/);assert.match(read('public/experience/2/js/core/preferences.js'),/growth-compass:preview2:e2:/)});
function*walk(dir){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const file=path.join(dir,entry.name);if(entry.isDirectory())yield*walk(file);else yield file}}
